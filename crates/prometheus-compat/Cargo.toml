[package]
name = "prometheus"
version = "0.13.5"
edition.workspace = true
license.workspace = true
rust-version.workspace = true
publish = false

[dependencies]
prometheus-upstream = { package = "prometheus", version = "0.14.0", default-features = false }

[features]
default = ["protobuf"]
protobuf = ["prometheus-upstream/protobuf"]

[lints]
workspace = true
